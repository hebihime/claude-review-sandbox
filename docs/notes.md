# Scratch notes

Fixture only. This file exists to be matched by the `docs/**` glob in
.claude-review.yml, proving the action reads the repository's own ignore
patterns and not just its built-in list.
